## 添加依赖的规则

当需要添加依赖时，必须使用 `pnpm add` 或者 `cargo add` 命令。除非必要，在命令中不要指定依赖的版本。不得修改 `package.json` 或者 `Cargo.toml` 文件中的依赖项。需要移除依赖时同理。

注意，使用 cargo 命令时注意工作目录。

## 前端编写说明

如果遇到属性排序错误，不需要您手动修复，运行 `pnpm lint:fix` 即可自动修复。
